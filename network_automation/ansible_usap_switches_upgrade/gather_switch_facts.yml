---

- name : GATHER SWITCH FACTS
  connection: network_cli
  hosts: arltx-2960x-sw01 dtx-mrt-6th-stack-2960x-sw01
  gather_facts: false


  tasks:
    - name: Gather all legacy facts
      ios_facts:
        gather_subset: hardware
      register: output
      tags:
        - facts
    - debug:
        var: output  
