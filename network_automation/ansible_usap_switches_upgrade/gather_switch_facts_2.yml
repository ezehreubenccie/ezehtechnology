---

- name : GATHER SWITCH FACTS
  connection: network_cli
  hosts: usapswitches
  gather_facts: false


  tasks:
    - name: Gather all legacy facts
      ios_facts:
        gather_subset: hardware
      register: output
      tags:
        - facts
    - debug:
        var: output  
