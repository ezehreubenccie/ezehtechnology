#!/usr/bin/env python

def ssh_conn(ip_addr, username, password):
    print('IP address is: {}'.format(ip_addr))
    print('Username is: {}'.format(username))
    print('Password is: {}'.format(password))
    return

ssh_conn('10.2.254.43', 'ad_reuben.ezeh', 'cisco234!')
