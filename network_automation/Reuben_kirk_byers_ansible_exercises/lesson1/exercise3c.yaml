---
- name: Exercise 3a
  hosts: localhost:cisco
  gather_facts: False  


  tasks:
    - ansible.builtin.debug:
        var: ansible_host

    - ansible.builtin.debug:
        var: ansible_connection
